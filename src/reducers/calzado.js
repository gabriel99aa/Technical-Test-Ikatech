import { createSlice } from '@reduxjs/toolkit';

export const calzadoSlice = createSlice({
    name: 'calzado',
    initialState: {
        list: []
    },
    reducers: {

    }
})

export default calzadoSlice.reducer;